-- Migration 0051: Системный актор в ленте изменений
--
-- Автоматизации (автооценка, авто-отказ по AI-скору) пишут события
-- от имени системы: actor_id = NULL. Ленты отображают такие события
-- как «Система». FK на user.id сохраняется для ненулевых значений.

ALTER TABLE "activity_log" ALTER COLUMN "actor_id" DROP NOT NULL;

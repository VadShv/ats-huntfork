-- Migration 0050: Переименование этапа «Собеседование» → «Интервью»
--
-- Единая терминология продукта: везде используется слово «Интервью»
-- (кнопки, локали, планировщик). Переименовываем системный root-этап
-- hh-воронки, оставшийся со старым названием. Пользовательские этапы
-- с другими именами не трогаем.

UPDATE "pipeline_stage" SET "name" = 'Интервью' WHERE "name" = 'Собеседование' AND "type" = 'interview';
